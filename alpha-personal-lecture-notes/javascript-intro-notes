// Javascript Intro 2/15/2023

// JavaScript is a dynamic, interpreted, scripting language

//dynamic: you can create a variable and give it any data type and you 
//can change the data type later if you want

//interpreted:  code is read line by line and passed to an interpreter that creates the 
//machine code

//scripting: allows us to perform logic and manipulate data

// Data Types

//primitive data type: cant be broken down any further

//number
//string
//Boolean
//undefined
//null
//symbol

//number
42 //whole number
3.14 //float

5 + 4
console.log(5 + 4)
5 - 3
console.log(5 - 3)
5 * 4
console.log(5 * 4)
15 / 3
console.log(15 / 3)
// $ node javascript-intro.js

console.log( 5 % 3) //modulo divides numbers than returns remainder
console.log(4 ** 3) //exponential

//string
console.log("hello alpha")
//strings have a length property
//index: location of a character inside the string
//zero indexing: counting indexes, the first location is 0
console.log("hello world"[1])
//bracket notion will allow you to pass an index and return the 
//value at that index

//Boolean
true
false
console.log(true)

//undefined: a variable that doesnt have a value, if there isnt a value 
//the falye will be undefined

//null: value that is equal to nothing else

//symbol: unique key used as an identifier

//variables

//variable declaration
//var: most generic, global scope
//let: local scope, live inside some piece of code functionality
//const: will protect the variable from being reassigned

//variable names: always communicate intent, in camelCase

//thisIsMyVariableInCamelCase

//assignment operator
var myCohort = "Alpha class 2023"
console.log(myCohort)

var heyThere
console.log(heyThere) //--> undefined

//console.log(hiAlpha) //-- not defined

heyThere = "hey there everyone!"
console.log(heyThere)
console.log(heyThere[0])

heyThere = 29
console.log(heyThere)
console.log(heyThere % 3)

//Built-in Methods

console.log("hello".length) //.length counts the number of characters in a string

const helloWorld = "Hey There!!"
console.log(helloWorld.length) //.length counts the string assigned to helloWorld
console.log(helloWorld[0]) //[0] will output the first character at index 0

console.log(helloWorld.toUpperCase()) //outputs the string in all upper case letters
//parenthesis = action

console.log(helloWorld.includes("e")) //.includes will search the assgined string for the passed argument
//extra info that gets passed into the parentheses is called an argument
//.includes returns a boolean value 

console.log(helloWorld.slice(0, 3)) //.slice will output a section of a string 
//passed in the argument is the start and last index places